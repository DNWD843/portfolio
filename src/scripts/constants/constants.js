export const privateLinksList = document.querySelectorAll('#private');
export const popupElement = document.querySelector('#popup');
export const popupCloseButton = document.querySelector('#close-popup');
export const CARD_SELECTORS_LIST = {
  titleSelector: '.card__title',
  subtitleSelector: '.card__subtitle',
  imageSelector: '.card__image',
  overlaySelector: '.card__overlay',
  linkSelector: '.card__link',
};

export const CARD_TEMPLATE_SELECTOR = '#card-template';
export const CARD_SELECTOR = '.cards__item';
export const testWorksContainer = document.querySelector('#testworks .cards');
